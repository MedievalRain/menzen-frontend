export interface AuthData {
  email: string;
  password: string;
}
export interface RenameTableData {
  name: string;
  id: string;
}
export interface Table {
  id: string;
  name: string;
  createdAt: string;
}
